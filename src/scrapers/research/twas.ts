import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://twas.org/opportunities";

export const scrapeTWASOpportunities: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

