import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL =
  "https://www.gatesfoundation.org/about/committed-grants";

export const scrapeGatesFoundationGrants: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

