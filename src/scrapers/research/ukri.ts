import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.ukri.org/opportunity/";

export const scrapeUKRIFundingFinder: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

