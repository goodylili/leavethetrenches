import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://techpoint.africa/events";

export const scrapeTechpointAfricaEvents: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

