import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://developers.google.com/community";

export const scrapeGDGAndDSCEvents: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

