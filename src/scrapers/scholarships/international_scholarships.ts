import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.internationalscholarships.com";

export const scrapeInternationalScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

