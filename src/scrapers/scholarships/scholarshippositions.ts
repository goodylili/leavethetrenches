import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.scholarshippositions.com";

export const scrapeScholarshipPositionsScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

