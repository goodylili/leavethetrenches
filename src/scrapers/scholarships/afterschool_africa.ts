import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.afterschoolafrica.com";

export const scrapeAfterSchoolAfricaScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};
