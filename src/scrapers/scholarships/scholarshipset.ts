import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.scholarshipset.com";

export const scrapeScholarshipSetScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

