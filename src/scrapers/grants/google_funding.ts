import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://startup.google.com";

export const scrapeGoogleFundingPrograms: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

