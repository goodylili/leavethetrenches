import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.usaid.gov/work-usaid";

export const scrapeUSAIDGrantsAndContracts: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

