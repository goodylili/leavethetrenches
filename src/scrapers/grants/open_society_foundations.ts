import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.opensocietyfoundations.org/grants";

export const scrapeOpenSocietyFoundationsGrants: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

