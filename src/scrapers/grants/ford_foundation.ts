import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.fordfoundation.org/work/our-grants/";

export const scrapeFordFoundationGrants: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

