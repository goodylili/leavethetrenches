import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.openbugbounty.org";

export const scrapeOpenBugBounty: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

