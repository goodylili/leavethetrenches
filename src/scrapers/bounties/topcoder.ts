import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.topcoder.com/challenges";

export const scrapeTopcoderBounties: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

