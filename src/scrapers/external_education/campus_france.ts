import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.campusfrance.org/en";

export const scrapeCampusFrancePrograms: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

