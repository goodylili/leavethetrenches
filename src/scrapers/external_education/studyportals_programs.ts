import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.studyportals.com";

export const scrapeStudyportalsPrograms: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

