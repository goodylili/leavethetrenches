import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.chevening.org/scholarships/";

export const scrapeCheveningScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

