import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://educationusa.state.gov";

export const scrapeEducationUSAGlobalPrograms: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

