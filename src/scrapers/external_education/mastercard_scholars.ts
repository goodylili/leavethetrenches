import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://mastercardfdn.org/all/scholars/";

export const scrapeMastercardFoundationScholars: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

