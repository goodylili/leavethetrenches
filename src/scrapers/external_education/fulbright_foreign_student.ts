import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://foreign.fulbrightonline.org";

export const scrapeFulbrightForeignStudentProgram: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

