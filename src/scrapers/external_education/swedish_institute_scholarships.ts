import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://si.se/en/apply/scholarships/";

export const scrapeSwedishInstituteScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

