import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://cscuk.fcdo.gov.uk";

export const scrapeCommonwealthScholarships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

