import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.atlanticfellows.org/programmes";

export const scrapeAtlanticFellows: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

