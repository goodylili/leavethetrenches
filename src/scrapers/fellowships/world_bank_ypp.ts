import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL =
  "https://www.worldbank.org/en/about/careers/programs-and-internships";

export const scrapeWorldBankYPPAndFellowships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

