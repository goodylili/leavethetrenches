import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.schwarzmanscholars.org";

export const scrapeSchwarzmanScholars: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

