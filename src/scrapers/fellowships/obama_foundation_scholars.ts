import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.obama.org/scholars/";

export const scrapeObamaFoundationScholars: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

