import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.afterschoolafrica.com/tag/fellowships/";

export const scrapeAfterSchoolAfricaFellowships: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

