import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://leetcode.com/contest/";

export const scrapeLeetCodeContests: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

