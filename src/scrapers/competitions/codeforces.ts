import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://codeforces.com/contests";

export const scrapeCodeforcesContests: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

