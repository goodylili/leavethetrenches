import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.kaggle.com/competitions";

export const scrapeKaggleCompetitions: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

