import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.ziprecruiter.com";

export const scrapeZipRecruiterJobs: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

