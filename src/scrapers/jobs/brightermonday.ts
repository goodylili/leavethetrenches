import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://www.brightermonday.co.ke";

export const scrapeBrighterMondayJobs: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

