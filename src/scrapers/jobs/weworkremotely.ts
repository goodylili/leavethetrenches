import { Opportunity, SiteScraper } from "../types";

const SOURCE_URL = "https://weworkremotely.com";

export const scrapeWeWorkRemotelyJobs: SiteScraper = async () => {
  const opportunities: Opportunity[] = [];

  return opportunities;
};

